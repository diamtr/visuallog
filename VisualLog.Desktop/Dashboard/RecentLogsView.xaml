<UserControl x:Class="VisualLog.Desktop.Dashboard.RecentLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualLog.Desktop.Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      <DataTemplate DataType="{x:Type local:RecentLogViewModel}">
        <local:RecentLogView/>
      </DataTemplate>
    </ResourceDictionary>
  </UserControl.Resources>

  <Border Background="White"
          CornerRadius="5"
          Padding="10"
          MaxHeight="400">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- HEADER -->
      <Border Grid.Row="0" Margin="0,0,0,15">
        <TextBlock Text="Available Recent" FontWeight="Bold" TextWrapping="WrapWithOverflow" />
      </Border>

      <!-- LOGS -->
      <ListView Grid.Row="1"
                x:Name="RecentLogsList"
                ItemsSource="{Binding RecentLogs}"
                BorderThickness="0"
                ItemContainerStyle="{StaticResource AvoidSelectionListViewItem}"
                HorizontalContentAlignment="Stretch"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
      </ListView>
    </Grid>
  </Border>
</UserControl>
