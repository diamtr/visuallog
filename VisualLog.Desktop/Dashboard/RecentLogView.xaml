<UserControl x:Class="VisualLog.Desktop.Dashboard.RecentLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualLog.Desktop.Dashboard"
             xmlns:conv="clr-namespace:VisualLog.Desktop"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400" d:Background="White">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      <conv:VisibilityBoolConverter x:Key="VisibilityBoolConverter"/>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid>

    <Grid Margin="35,0,0,0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" TextWrapping="WrapWithOverflow" Text="{Binding LogInfo.DisplayName, Mode=OneWay}" />
      <TextBlock Grid.Row="1" TextWrapping="WrapWithOverflow" Text="{Binding LogInfo.PathDirectory, Mode=OneWay}" Foreground="Gray" FontSize="10" />
      <TextBlock Grid.Row="2" TextWrapping="WrapWithOverflow" Text="{Binding LogInfo.LastOpened, Mode=OneWay, StringFormat='yyyy-MM-dd HH:mm', TargetNullValue=''}" Foreground="Gray" FontSize="10" />
    </Grid>

    <DockPanel>

      <Button DockPanel.Dock="Right"
              Content="open"
              Command="{Binding OpenCommand}"
              Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=DockPanel}, Converter={StaticResource VisibilityBoolConverter}, Mode=OneWay, FallbackValue=Collapsed}"
              Style="{StaticResource LinkButton}"/>

      <CheckBox DockPanel.Dock="Left"
                Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=DockPanel}, Converter={StaticResource VisibilityBoolConverter}, Mode=OneWay, FallbackValue=Collapsed}"
                Margin="10,0,0,0"
                VerticalAlignment="Center"/>

      <Border BorderThickness="0" BorderBrush="Transparent" Background="Transparent" />

    </DockPanel>
  </Grid>
  
  
</UserControl>
