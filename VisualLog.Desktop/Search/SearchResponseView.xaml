<UserControl x:Class="VisualLog.Desktop.Search.SearchResponseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualLog.Desktop.Search"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="450" d:Background="White">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      <DataTemplate DataType="{x:Type local:LogSearchResultsViewModel}">
        <local:LogSearchResultsView/>
      </DataTemplate>
    </ResourceDictionary>
  </UserControl.Resources>
  
  <Grid>
    <ItemsControl ItemsSource="{Binding LogSearchResults}"
                  Style="{StaticResource SpacedVerticalItemsControl}"/>
  </Grid>
</UserControl>
