<UserControl x:Class="VisualLog.Desktop.Dashboard.OpenedLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualLog.Desktop.Dashboard"
             xmlns:logmgr="clr-namespace:VisualLog.Desktop.LogManager"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>

      <DataTemplate DataType="{x:Type logmgr:LogViewModel}">
        <local:OpenedLogView/>
      </DataTemplate>
    </ResourceDictionary>
  </UserControl.Resources>

  <Border Background="White"
          CornerRadius="5"
          Padding="10">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- HEADER -->
      <Border Grid.Row="0" Margin="0,0,0,15">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="Logs" FontWeight="Bold" TextWrapping="WrapWithOverflow" />
          <Button Grid.Column="1" Content="Open" Command="{Binding DashboardViewModel.MainViewModel.OpenLogsCommand}" Style="{StaticResource LinkButton}"/>
        </Grid>
      </Border>

      <!-- LOGS -->
      <ListView Grid.Row="1"
                ItemsSource="{Binding DashboardViewModel.MainViewModel.LogManagerViewModel.Logs}"
                BorderThickness="0"
                ItemContainerStyle="{StaticResource AvoidSelectionListViewItem}">
      </ListView>
    </Grid>
  </Border>
  
</UserControl>
