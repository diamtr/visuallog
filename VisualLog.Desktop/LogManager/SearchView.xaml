<UserControl x:Class="VisualLog.Desktop.LogManager.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualLog.Desktop"
             xmlns:logmgr="clr-namespace:VisualLog.Desktop.LogManager"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <ResourceDictionary>

      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>

      <local:VisibilityCountConverter x:Key="VisibilityCountConverter"/>

      <DataTemplate DataType="{x:Type logmgr:MessageInlineViewModel}">
        <logmgr:MessageInlineView />
      </DataTemplate>

      <DataTemplate DataType="{x:Type logmgr:SearchEntryViewModel}">
        <logmgr:SearchEntryView />
      </DataTemplate>

    </ResourceDictionary>
  </UserControl.Resources>

  <UserControl.InputBindings>
    <KeyBinding Gesture="Esc" Command="{Binding HideSearchPanelCommand}"/>
    <KeyBinding Gesture="Enter" Command="{Binding SearchCommand}" CommandParameter="{Binding Text, ElementName=StringToSearch}"/>
    <KeyBinding Gesture="Ctrl+C" Command="{Binding CopySelectedCommand}" CommandParameter="{Binding SelectedItems, ElementName=SearchEntriesList}"/>
  </UserControl.InputBindings>

  <Border>
    <Border Margin="10,10,5,5">
      <Grid Grid.IsSharedSizeScope="True">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <Button Grid.Column="0" Style="{StaticResource ActionButton}" Padding="2" Margin="0,0,15,0" VerticalAlignment="Center"
                  Command="{Binding SearchCommand}" CommandParameter="{Binding Text, ElementName=StringToSearch}" Height="18">
            <svgc:SvgViewbox Source="pack://application:,,,/img/SearchView/refresh.svg"/>
          </Button>
          <TextBox Grid.Column="1" x:Name="StringToSearch" Text="{Binding StringToSearch}" Height="20"/>
          
        </Grid>
        

        <Grid Grid.Row="1">
          <StackPanel Orientation="Horizontal">
            <ToggleButton VerticalAlignment="Center"
                          Content="RegEx"
                          IsChecked="{Binding UseRegex}"
                          Style="{StaticResource OptionToggleButton}"
                          FontSize="10" FontStyle="Italic" Margin="3,3,15,3" Padding="10,0"/>
            <TextBlock Foreground="Gray"
                       FontSize="10" FontStyle="Italic" VerticalAlignment="Center">
              <Run Text="Found:"/>
              <Run Text="{Binding SearchEntries.Count, Mode=OneWay}"/>
              <Run Text="lines."/>
              <Run Text="{Binding LastSearchDateTime, Mode=OneWay, StringFormat='yyyy-MM-dd HH:mm:ss', TargetNullValue=''}"/>   
            </TextBlock>
          </StackPanel>
        </Grid>

        <Grid Grid.Row="2" Visibility="{Binding SearchEntries.Count, Converter={StaticResource VisibilityCountConverter}, Mode=OneWay}">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <Border Grid.Column="1" Margin="0,0,0,5" Style="{StaticResource ActionButtonBorder}">
            <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
              <Button Style="{StaticResource ActionButton}" Content="View All" Command="{Binding ViewAllCommand}"/>
              <Button Style="{StaticResource ActionButton}" Content="View Selected" Command="{Binding ViewSelectedCommand}" CommandParameter="{Binding SelectedItems, ElementName=SearchEntriesList}"/>
              <Button Style="{StaticResource ActionButton}" Content="Copy All" Command="{Binding CopyAllCommand}"/>
              <Button Style="{StaticResource ActionButton}" Content="Copy Selected" Command="{Binding CopySelectedCommand}" CommandParameter="{Binding SelectedItems, ElementName=SearchEntriesList}"/>
              <Button Style="{StaticResource ActionButton}" Content="Clear" Command="{Binding ClearCommand}"/>
            </StackPanel>
          </Border>
        </Grid>
        
        <Grid Grid.Row="3" Visibility="{Binding SearchEntries.Count, Converter={StaticResource VisibilityCountConverter}, Mode=OneWay}">
          <ListView x:Name="SearchEntriesList"
          ItemsSource="{Binding SearchEntries}"
          SelectionMode="Extended"
          MinHeight="100">
            <ListView.Resources>
              <Style TargetType="{x:Type ListViewItem}">
                <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
              </Style>
            </ListView.Resources>
          </ListView>
        </Grid>

      </Grid>
    </Border>
  </Border>
</UserControl>
