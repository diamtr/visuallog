<UserControl x:Class="VisualLog.Desktop.FormatManager.FormatListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualLog.Desktop.FormatManager"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="110">
  <Grid>
    
    <Border Margin="5,3,3,3"
            BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="3"
            Height="45"
            MaxWidth="125"
            MinWidth="75">
      <TextBlock Text="{Binding Format.Name}"
                 TextWrapping="Wrap"/>
    </Border>
    
  </Grid>
</UserControl>
